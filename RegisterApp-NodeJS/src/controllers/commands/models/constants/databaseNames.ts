export enum DatabaseTableName {
	PRODUCT = "product"
}
/***************************************/

export enum ProductFieldName {
	ID = "id",
	COUNT = "count",
	CREATED_ON = "createdon",
	LOOKUP_CODE = "lookupcode"
}
